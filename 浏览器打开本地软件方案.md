# 浏览器安全限制下打开本地软件的解决方案

## 问题背景

网页（浏览器）出于安全沙箱限制，**无法直接启动本机软件**（如 SumatraPDF、WPS、Typora 等）。
因此「论文详情 → 用自定义软件打开 PDF」在纯网页环境下只能退化为「下载 PDF 由系统关联程序打开」。

## 方案总览

| 方案 | 体验 | 需要条件 | 适用 |
|---|---|---|---|
| A. 本地桥接服务（**推荐**） | 一键跳转自定义软件 | 本机安装 Node.js，运行一个小服务 | 网页版 + 自定义软件 |
| B. 桌面版（Tauri/Electron） | 原生体验 | 使用打包好的桌面程序 | 日常高频使用 |
| C. 下载兜底（当前默认） | 手动打开 | 无 | 任何情况 |

## 方案 A：本地桥接服务（网页版推荐）

### 原理

浏览器虽然不能启动本地软件，但**允许网页访问 `http://127.0.0.1`（本机回环地址）**。
因此在本机运行一个极小的 HTTP 服务：

1. 网页把 PDF 二进制 POST 给 `http://127.0.0.1:39330/open-blob`
2. 服务把 PDF 保存到本地目录（默认 `文档/WorkbenchPDF`，**可在设置页自定义**，也可手动编辑 `bridge-config.json`）
3. 服务调用你配置的软件（`spawn("exe", [pdf路径])`）或系统默认程序打开

### 使用方法（Windows）

1. 确保电脑已安装 [Node.js](https://nodejs.org)（LTS 即可，装一次即可）
2. 在「平台设置 → 偏好设置 → PDF 打开方式」选择「自定义软件」并保存你的阅读器路径
3. 在设置页点击下载「server.cjs」和「启动桥接服务.bat」，放到同一文件夹
4. 双击「启动桥接服务.bat」，保持窗口开启（可最小化）
5. 回到设置页点「重新检测」，显示「服务在线」即配置成功

之后在论文库点 PDF 按钮，选择自定义软件打开方式，就会直接调用你的软件打开 PDF。

### 自定义保存目录

- 在「平台设置 → 偏好设置 → PDF 打开方式」的「PDF 保存目录」输入框填写绝对路径（如 `D:\Papers\PDF`）并保存
- 服务在线时会自动写入 server.cjs 同目录的 `bridge-config.json`，之后网页打开 PDF 即存入该目录
- 也可手动编辑 `bridge-config.json`：`{ "saveDir": "D:\\Papers\\PDF" }`，保存后重启服务生效
- 保存目录优先级：网页调用参数 > `bridge-config.json` > 默认 `文档/WorkbenchPDF`

### 手动启动（macOS / Linux）

```bash
node server.cjs
```

### 接口说明

- `GET /ping`：探活，返回当前保存目录
- `GET /config`：读取保存目录配置
- `POST /config`：写入保存目录配置（body 为 `{"saveDir": "D:\\Papers\\PDF"}`）
- `GET /find-apps`：自动检测本机已安装的 PDF 阅读器，返回 `[{ name, path }]`（SumatraPDF / Adobe Acrobat / Reader / Foxit / PDF-XChange / WPS / 万兴 PDFelement / Edge / Chrome / Firefox 等）
- `GET /check-app?path=xxx`：校验软件路径是否有效，返回 `{ found, resolved }`
- `POST /open-blob?name=xx.pdf&app=可选程序路径&saveDir=可选保存目录`：接收 PDF 二进制，保存并用软件打开
- 端口 `39330`，如需修改请编辑 server.cjs 顶部的 `PORT`

### 填写自定义软件路径（重要）

网页端受浏览器安全限制，「浏览」选文件**只能拿到文件名、拿不到完整路径**，直接保存会报「软件不存在」。
正确做法（三选一）：

1. **自动检测（推荐）**：设置页点「自动检测」，服务会扫描本机已装的 PDF 阅读器，点击结果即自动填入完整路径并保存
2. **手动填写完整路径**：如 `C:\Program Files\SumatraPDF\SumatraPDF.exe`（不带引号）
3. **只填软件名**：如 `SumatraPDF`，服务会自动到安装目录 / PATH 中查找（支持 `chrome`、`notepad` 等）

保存前可点「验证」实时校验路径是否有效。服务端还支持带引号路径、`%ProgramFiles%` 环境变量、只填目录（自动补 exe）、`.lnk` 快捷方式。

## 方案 B：桌面版

平台已提供 Tauri 桌面版打包。桌面版由系统授权，可以原生调用任意本地软件打开 PDF，
无需桥接服务。建议日常使用桌面版，网页版用作移动端/临时访问。

## 方案 C：下载兜底

网页端未检测到桥接服务在线时，自动回退为下载 PDF，由系统关联程序打开——功能不丢失，仅多一步手动打开。

---

> 安全性说明：桥接服务仅监听本机 127.0.0.1，不对外网开放；只接收「保存 PDF 并调用本地程序打开」这一种指令，不执行任意命令、不读写任意路径，可放心使用。
