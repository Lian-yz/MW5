# 研究生工作台 · 阿里云 .cn 域名部署方案

> 目标：把平台部署到你自己注册的 **.cn 域名**，国内访问快、稳定。
> 平台是 Vue3 纯静态前端（hash 路由、无后端），部署 = 静态文件托管 + 域名解析 + HTTPS。
> 关键前提：**.cn 是国内域名，必须完成 ICP 备案才能解析到国内服务器/CDN**。

---

## 0. 方案总览与成本

### 两种子方案（按需选）

| 子方案 | 托管方式 | 备案接入 | 适合 |
|---|---|---|---|
| **B1（推荐）** | OSS 存储 + CDN 加速 | 轻量应用服务器（仅出备案服务号） | 想要最好访问体验、可弹性扩容 |
| **B2（最省）** | 轻量应用服务器 + Nginx 直接托管 | 同一台轻量服务器 | 预算最低、一台机器全包 |

> 纯 OSS **无法单独备案**——阿里云备案要求有一个云服务器（ECS / 轻量应用服务器等）提供「备案服务号」。所以无论 B1/B2，都需要至少一台轻量服务器（最便宜档即可）。

### 年成本估算（小项目量级，价格以阿里云官网实时为准）

| 项目 | 费用 | 说明 |
|---|---|---|
| .cn 域名 | 约 29–45 元/年 | 阿里云常促销首年 29 元，续费约 39–45 元 |
| 轻量应用服务器（备案用，最低配 2核1G） | 约 60–90 元/月，年付约 600–1000 元 | B1 仅用于出备案服务号；B2 同时跑站点 |
| OSS 标准存储 | 几元/月 | B1 用，存储 + 少量请求 |
| CDN 流量 | 几元到十几元/月 | B1 用，可买流量包 |
| SSL 证书 | 0 元 | 阿里云免费 DV 证书（单域名，一年期） |
| ICP 备案 | 0 元（耗时 1–2 周） | 需人脸核验 |
| **B1 合计** | 约 900–1100 元/年 | 域名 + 轻量 + OSS + CDN |
| **B2 合计** | 约 650–1050 元/年 | 域名 + 轻量（含托管，不买 OSS/CDN） |

> 想最省钱走 **B2**：一台轻量服务器既出备案服务号又直接托管静态文件，省掉 OSS/CDN。

---

## 1. 注册 .cn 域名（阿里云）

1. 登录 **阿里云控制台** → 进入「域名」→「域名注册」。
2. 搜索你想要的 `.cn` 域名，加入清单并下单购买。
3. **实名认证（必做）**：
   - 个人：提交身份证信息，1 个工作日内审核。
   - 企业：提交营业执照。
   - ⚠️ 未实名的 .cn 域名会被注册局锁定，无法解析。
4. 实名通过后，域名状态变为「正常」，可进行后续解析。

---

## 2. ICP 备案（用轻量服务器出备案服务号）

> 不备案，.cn 域名无法解析到国内节点（会触发拦截）。备案免费但约 1–2 周。

### 2.1 购买轻量应用服务器（备案接入资源）

1. 控制台 →「轻量应用服务器」→ 创建实例。
2. 地域选**国内**（如华东1 杭州 / 华北2 北京）。
3. 镜像选 **应用镜像 → Nginx**（B2 直接用它托管；B1 仅备案用，选什么镜像都行）。
4. 规格选**最低配**（2核1G 或 2核2G）即可，省钱。
5. 完成购买。

### 2.2 提交备案

1. 控制台 →「备案」→「ICP 备案」（或直接访问 beian.aliyun.com）。
2. 按向导：
   - 填写主体信息（个人：姓名/身份证/手机号；人脸核验）。
   - **备案服务号**：在轻量服务器控制台「备案」入口生成该实例的备案服务号，填到备案系统。
   - 填写网站信息：域名填你的 `.cn`，网站名称填「研究生工作台」之类。
3. 阿里云初审（1–2 天）→ 提交管局（各省通信管理局，约 1–2 周）。
4. 管局审核通过，获得 **ICP 备案号**（如「沪ICP备XXXXXXXX号-X」）。
5. ⚠️ 合规要求：网站首页底部需悬挂备案号并链接到 `https://beian.miit.gov.cn`。

> 备案期间域名不能提前解析到国内 IP（会被阻断）。可先准备文件，备案通过后再解析上线。

---

## 3. 准备构建产物

在本地项目根目录：

```bash
npm install
npx vite build --outDir dist_v5153
```

`dist_v5153/` 内全部文件即为上线文件（确保 `index.html` 在根）。

---

## 4. 子方案 B1：OSS + CDN

### 4.1 开通 OSS 并上传

1. 控制台 →「对象存储 OSS」→ 创建 Bucket：
   - 地域：与轻量同地域（国内）。
   - 读写权限：**公共读**。
   - 关闭「阻止公共访问」（否则公共读不生效）。
2. Bucket →「数据管理 → 静态页面」：
   - 默认首页：`index.html`
   - 默认 404 页：`index.html`（hash 路由用不到，但填上更稳）。
3. 把本地 `dist_v5153/` **全部内容**上传到 Bucket 根目录。

### 4.2 开通 CDN 加速

1. 控制台 →「CDN」→「添加加速域名」：
   - 加速域名：填你的 `.cn`（如 `www.你的域名.cn` 或根域名）。
   - 业务类型：图片小文件 / 全站加速均可。
   - 源站类型：**OSS**，回源到刚才 Bucket 的访问域名。
2. 添加后，CDN 分配一个 **CNAME**（如 `*.kunlun*.com`）。
3. 「HTTPS」配置 → 申请**免费 DV 证书**（阿里云证书服务，单域名一年期），开启「强制 HTTPS 跳转」。
4. 等待证书签发、配置生效。

### 4.3 域名解析

1. 域名控制台 →「解析」→ 添加记录：
   - 主机记录：`www`（或 `@` 根域名）
   - 记录类型：**CNAME**
   - 记录值：CDN 分配的 CNAME 地址。
2. 若用根域名 `@` 解析，部分场景 CNAME 与 MX 冲突，建议用 `www.你的域名.cn` 并把根域名 301 跳转到 www（在 CDN/解析里配置）。

---

## 5. 子方案 B2：轻量服务器 + Nginx 直接托管

> 最省方案：一台轻量服务器同时出备案服务号 + 跑站点，不买 OSS/CDN。

1. 轻量服务器控制台 → 防火墙 → 放行 **80 / 443** 端口（TCP）。
2. 通过「远程连接」或 SSH 登录服务器（镜像已带 Nginx）。
3. 上传 `dist_v5153/` 内容到网站根目录（Nginx 应用镜像默认 `/usr/share/nginx/html`）：
   ```bash
   # 本地把 dist_v5153 打成 zip，传到服务器后解压覆盖到 html 目录
   scp -r dist_v5153/* root@<服务器公网IP>:/usr/share/nginx/html/
   ```
4. Nginx 静态托管默认已配置根目录，直接生效。确认 `index.html` 在根。
5. **HTTPS**：
   - 轻量控制台「SSL 证书」申请免费 DV 证书，绑定域名并「部署到 Nginx」。
   - 或在证书服务申请后，下载证书配置到 Nginx 的 `nginx.conf`（开启 443 + 强制跳转）。
6. **域名解析**：域名控制台 → 解析 → 添加：
   - 主机记录：`www`（或 `@`）
   - 记录类型：**A**
   - 记录值：轻量服务器的**公网 IP**。
7. 备案通过后，访问你的 `.cn` 即可。

> 轻量服务器带宽一般为固定峰值（如 3–5 Mbps），个人使用足够；如需更高并发再升级规格或转 B1。

---

## 6. 数据迁移（重要）

1. **旧沙箱环境**：平台「设置 → 导出/备份」下载 JSON。
2. **新 .cn 环境**：部署完成后首次打开 →「设置 → 导入备份」上传 JSON。
3. 刷新，数据恢复。

> 数据在浏览器 localStorage，按域名隔离，换域名必须走导出/导入。

---

## 7. 常见坑

- ❌ **.cn 未实名** → 域名被锁定无法解析。先完成实名。
- ❌ **未备案就解析到国内 IP** → 被阻断。备案通过再解析。
- ❌ **OSS 公共读被「阻止公共访问」关掉** → 页面 403。创建 Bucket 时关闭该开关。
- ❌ **上传时把 dist_v5153 文件夹本身传上去** → 导致 `域名/dist_v5153/index.html`。应传**目录内内容**，让 `index.html` 在站点根。
- ❌ **根域名 @ 用 CNAME 与 MX 冲突** → 用 `www` 子域，或 CDN 支持 CNAME 扁平化时再处理。
- ⚠️ **备案号未悬挂** → 管局抽查会要求整改。首页底部加备案号链接。
- ⚠️ 平台是 hash 路由，无需配 SPA 的 404 重写；若以后切 history 路由才需要回退到 index.html。

---

## 8. 我的建议

- 预算宽松、要最好体验 → **B1（OSS + CDN）**，轻量服务器仅备案用。
- 预算优先、能接受单机带宽 → **B2（轻量 + Nginx）**，一台机器全包，最省。
- 不论哪种，先把 **.cn 实名 + 备案** 启动（最耗时），备案期间并行准备文件和方案。

需要的话我可以：① 把 `dist_v5153/` 打成可直接上传的 zip 包；② 你选定 B1/B2 后，补一份带控制台截图位置的逐项核对清单。
