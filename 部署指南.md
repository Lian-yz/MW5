# 硕士工作台 · 自建域名部署指南

> 当前平台部署在 WorkBuddy 云端沙箱：`https://d71cb2a4119d49909efb171f9cd68134.bj7.agentos-app.net/`
> 本指南说明如何把同一套代码部署到**你自己的域名**。

---

## 0. 前置说明（必读）

- **平台性质**：Vue3 纯前端（vite 构建），**没有任何后端**。部署就是把构建产物当静态文件托管出去。
- **路由模式**：`createWebHashHistory`（hash 路由，URL 带 `#`）。好处是**部署到任何子目录都不需要 SPA fallback 重写**，普通静态托管即可，无需配 404 回退。
- **构建产物目录**：本地为 `dist_v5153/`（每次 build 用 `npx vite build --outDir dist_v5153`）。下面两条路线都会用到这个目录里的文件。
- **数据迁移（重要）**：所有数据存在浏览器 `localStorage`，**按域名隔离**。换域名后旧数据不会自动跟过来。迁移前请先在旧环境「设置 → 导出/备份」下载 JSON，部署到新域名后再「导入备份」。
- **备案约束**：只要域名解析到**中国大陆**的服务器/CDN，就必须做 ICP 备案。走境外服务（Cloudflare / GitHub Pages / Vercel 等）可免备案，但国内访问速度一般。

---

## 1. 准备构建产物

在本地项目根目录执行：

```bash
npm install
npx vite build --outDir dist_v5153
```

> 若沙箱的 safe-delete 防护拦截了清旧目录，可先 `mv dist_v5153 dist_v5153_old`，再 build；或直接覆盖写入（构建日志会有 chunk 体积警告，无错误即可）。

构建成功后，`dist_v5153/` 目录内即为可上线文件（`index.html` + `assets/` + `icons/` 等）。**上传时把这个目录里的全部内容传上去，确保 `index.html` 在站点根**。

> 若后续把平台放在子路径（如 `https://你的域名/workbench/`），需先把 `vite.config.js` 的 `base` 改为 `'/workbench/'` 再重新 build，否则资源会 404。本文默认部署在域名根目录。

---

## 2. 路线 A：Cloudflare Pages（推荐优先 · 免备案 · 免费）

适合：想最快上线、不愿走备案流程、能接受国内访问速度一般。

### 方式一：直接上传（最简单，无需 GitHub）

1. 注册 Cloudflare 账号（免费）。
2. 进入 **Workers & Pages → Create → Pages → 直接上传（Upload assets）**。
3. 把本地 `dist_v5153/` 里的全部内容拖进上传区，项目名随意（如 `master-workbench`）。
4. 部署完成后会得到一个 `*.pages.dev` 临时域名，先验证能正常打开。
5. **绑定自有域名**：项目 **Settings → Custom domains → 添加你的域名**。
   - 若域名 DNS 已在 Cloudflare 管理：自动签发免费 SSL，秒级生效。
   - 若域名在别的注册商：按提示把该域名的 CNAME 记录指向 `你的项目.pages.dev`，Cloudflare 自动签 SSL。
6. 等待 DNS 生效后访问你的域名即可。

### 方式二：连 GitHub 自动构建（改代码后自动上线）

1. 同路线把项目连到 GitHub 仓库（Cloudflare 授权 GitHub）。
2. 创建 Pages 项目时选择仓库，构建设置：
   - **Build command**：`npm install && npx vite build`
   - **Output directory**：`dist`（用 vite 默认输出，最省心）
3. 之后每次 `git push` 到 main，Cloudflare 自动重新构建部署。
4. 同样在 **Custom domains** 绑定你的域名。

**费用**：完全免费（含全球 CDN + SSL）。

---

## 3. 路线 B：阿里云 OSS + CDN（国内快 · 需备案）

适合：追求国内访问速度和稳定性，且愿意做 ICP 备案。

### 步骤

1. **备案**：在阿里云备案系统提交你的域名 ICP 备案（免费，约 1–2 周，需有备案接入的云资源——下面第 2 步的 OSS 绑定 ECS 或轻量服务器即可满足）。
2. **开通 OSS**：
   - 建 Bucket，地域选国内（如华东1），读写权限选「公共读」。
   - 开启「静态网站托管」，默认首页填 `index.html`，404 页也填 `index.html`（hash 路由其实用不到 404 回退，但填上更稳妥）。
3. **上传文件**：把 `dist_v5153/` 的全部内容上传到 Bucket 根目录。
4. **开通 CDN 加速**：
   - 加速域名填你的域名（如 `www.你的域名.com`）。
   - 源站类型选「OSS」，回源到该 Bucket 的访问域名。
   - 在 CDN 控制台「HTTPS 证书」申请**免费 DV 证书**并开启强制跳转 HTTPS。
5. **域名解析**：在你的域名注册商处，把域名 **CNAME** 指向 CDN 分配的 CNAME 地址。
6. 等 DNS 与备案生效后访问。

**费用（年/月，小项目量级）**：
- 域名：.cn 约 30–40 元/年，.com 约 60–70 元/年。
- OSS 存储：几元/月（存储 + 少量请求）。
- CDN 流量：按量，小项目月几元到十几元；可买资源包更省。
- 备案：免费（耗时）。
- 合计：约几十到几百元/年。

---

## 4. 数据迁移步骤

1. **旧沙箱环境**：进入平台「设置 → 导出/备份」，下载 `*.json` 备份文件。
2. **新域名环境**：部署完成后首次打开，进入「设置 → 导入备份」，上传该 JSON。
3. 刷新页面，数据即恢复。

> 提示：文件夹备份、GitHub tag 管理的是代码版本；用户业务数据只存在于浏览器 localStorage，迁移务必走上面的导出/导入。

---

## 5. 我的建议

| 你的诉求 | 推荐路线 |
|---|---|
| 最快上线、零成本、不想备案 | **Cloudflare Pages（路线 A）** |
| 国内访问快且稳定、愿花时间备案 | **阿里云 OSS+CDN（路线 B）** |
| 极简、纯托管、不碰构建 | 路线 A 的「直接上传」方式 |

如需，我可以帮你：把 `dist_v5153/` 整理成可直接拖拽上传的包、或针对你最终选定的平台出一份更细的截图级操作清单。

---

_生成于 v5.0.187 之后。平台当前为纯静态前端，无服务端依赖。_
